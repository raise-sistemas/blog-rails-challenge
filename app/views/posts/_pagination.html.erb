<% presenter = PaginationPresenter.new(page, posts) %>
<%= presenter.previous_pagination_condition { link_to "Previous Page", root_path(page: @page - 1), class: "button" } %>
<%= presenter.next_pagination_condition { link_to "Next Page", root_path(page: @page + 1), class: "button" } %>
